# NetDevOps Python — runtime dependencies
# Pin to compatible release ranges; lock to exact versions in CI via pip-compile / uv lock.

# Connection
netmiko>=4.3.0,<5.0
scrapli>=2024.7.0
ncclient>=0.6.13

# Structured config / data
jinja2>=3.1.0
pyyaml>=6.0
pydantic>=2.6.0

# HTTP APIs
httpx>=0.27.0
requests>=2.31.0

# Validation & parsing
jsonschema>=4.21.0
textfsm>=1.1.0
ntc-templates>=4.0.0

# Observability / telemetry
prometheus-client>=0.19.0
pysnmp>=6.2.0
